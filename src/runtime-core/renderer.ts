export function createRenderer<HostNode, HostElement>(options: any): any {
  // const { mount, patch } = renderOpsCreater(options);

  const render = (vnode, container) => {};

  const createApp = () => {};

  return {
    render,
    createApp,
  };
}
